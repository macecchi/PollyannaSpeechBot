machine:
    node:
        version: 6.10.0
    npm:
        version: 4.0.5
dependencies:
    override:
        - npm install --no-optional
deployment:
    production:
        branch: master
        commands:
            - npm run deploy
general:
    branches:
        only:
            - master